server:
  servlet:
    session:
      cookie:
        same-site: none
        secure: false # 로컬 개발을 위해 false로 변경
        # partitioned: true # secure: true 일 때만 의미가 있으므로 주석 처리
      timeout: 30m

spring:
  application:
    name: protin_cal
  datasource:
    url: jdbc:mysql://test-db.cvymy48u495a.ap-northeast-2.rds.amazonaws.com/workout?useSSL=false&allowPublicKeyRetrieval=true&useUnicode=true&serverTimezone=Asia/Seoul
    username: takch02
    password: tagtag1010
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true

logging:
  level:
    root=INFO: